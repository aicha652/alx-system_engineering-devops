#!/usr/bin/env bash
# Bash script that configures a new Ubuntu machine
sudo apt update

sudo apt install nginx

echo Hello World! > /var/www/html/index.nginx-debian.html
sed -i '80' /etc/nginx/sites-enabled/default
